{
  "name": "cuervito-lambda",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "prisma:generate": "prisma generate",
    "build": "npm run prisma:generate && tsc --project tsconfig.json && node scripts/stage.js",
    "bundle": "npm run build && cd build && zip -r ../function.zip .",
    "deploy": "npm run bundle && aws lambda update-function-code --function-name cuervito-photo-processor --zip-file fileb://function.zip",
    "deploy:create": "npm run bundle && aws lambda create-function --function-name cuervito-photo-processor --runtime nodejs20.x --role $LAMBDA_ROLE_ARN --handler handler.handler --zip-file fileb://function.zip --timeout 60 --memory-size 1024 --environment Variables=\"{DATABASE_URL=$DATABASE_URL,AWS_REGION=us-east-2}\""
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "prisma": "^6.6.0",
    "typescript": "^5.0.0"
  }
}
